# .env.example for moodle-cli
# Copy this file to .env and fill in your actual credentials.
# DO NOT commit the .env file to version control.

# -----------------------------------------------------------
# Environment: Production (`--env prod`)
# -----------------------------------------------------------
MOODLE_PROD_URL=https://your.moodle.site
MOODLE_PROD_USERNAME=your_admin_user
MOODLE_PROD_PASSWORD=your_super_secret_password

# --- Optional: CAS and Pre-defined Token for Production ---
# If your production Moodle uses CAS for SSO, uncomment and set the URL.
# MOODLE_PROD_CAS_URL=https://cas.your-institution.org/cas

# If you have a pre-defined webservice token (mandatory for CAS, optional otherwise).
MOODLE_PROD_WS_TOKEN=

# -----------------------------------------------------------
# Environment: Staging (`--env staging`)
# -----------------------------------------------------------
# Choose ONE of the sandbox options below.

# --- Option A: Moodle 5.0 Sandbox (Default for staging) ---
MOODLE_STAGING_URL=https://sandbox.moodledemo.net
MOODLE_STAGING_USERNAME=admin
MOODLE_STAGING_PASSWORD=sandbox24

# --- Option B: Moodle 4.5 Sandbox (Comment out the one above to use this) ---
# MOODLE_STAGING_URL=https://sandbox405.moodledemo.net
# MOODLE_STAGING_USERNAME=admin
# MOODLE_STAGING_PASSWORD=sandbox24

# --- Optional settings for Staging (usually not needed for sandboxes) ---
# MOODLE_STAGING_CAS_URL=
# MOODLE_STAGING_WS_TOKEN=

# -----------------------------------------------------------
# Environment: Local Development (`--env local`)
# -----------------------------------------------------------
MOODLE_LOCAL_URL=http://localhost
MOODLE_LOCAL_USERNAME=moodleuser
MOODLE_LOCAL_PASSWORD=PLEASE_CHANGEME

# --- Optional settings for Local (usually not needed for local environment) ---
# MOODLE_LOCAL_CAS_URL=
# MOODLE_LOCAL_WS_TOKEN=

# -----------------------------------------------------------
# How Environments Work
# -----------------------------------------------------------
# The CLI and tests use the --env flag to select a configuration.
# For example, `poetry run moodle-cli --env prod courses list` will use the MOODLE_PROD_* variables.
# If no --env flag is provided, it defaults to 'local'.



